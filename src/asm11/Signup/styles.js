import { StyleSheet } from "react-native";
export const style=StyleSheet.create({
    container:{
        flex: 1,
        marginStart: 24,
        marginEnd: 24,
        flexDirection: 'column',
    }


})